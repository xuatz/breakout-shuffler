node_modules
.pnpm-store

/.cache
/build
/public/build
.env
dist
claude-code-webui-linux-x64

# Local SSL certificates (generated by mkcert)
caddy/*.pem

# Docker Compose overrides (for local customizations)
docker-compose.override.yml
